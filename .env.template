
GITHUB_PAT=

# ===== POSTGRES SUPERUSER (Migration) =====
POSTGRES_DB_USER=postgres
POSTGRES_DB_PASSWORD=StrongSuperUserPasswordHere
POSTGRES_DB_NAME=postgres
POSTGRES_HOST=postgres

# AiAssistant — application DBs (tenant-readiness: <tenant>_<env>)
AGENT_DEV_DB_NAME=agent_dev
AGENT_DEV_DB_USER=agent_dev_user
AGENT_DEV_DB_PASSWORD=StrongAgentDevPassword
AGENT_LIVE_DB_NAME=agent_live
AGENT_LIVE_DB_USER=agent_live_user
AGENT_LIVE_DB_PASSWORD=StrongAgentLivePassword


# ===== Per-env agent DBs (workflow engine) =====
LANGGRAPH_DEV_DB_NAME=langgraph_dev
LANGGRAPH_LIVE_DB_NAME=langgraph_live


# Backwards-compatible aliases — app code (agents/workflow-runner) still reads
# AIASSIST_DB_* to build a fallback DATABASE_URL. Kept so app code is NOT
# modified for this rename; the composes set DATABASE_URL explicitly anyway.
# TODO: align app code to AGENT_DEV_DB_* and remove these aliases.
AIASSIST_DB_NAME=agent_dev
AIASSIST_DB_USER=agent_dev_user
AIASSIST_DB_PASSWORD=StrongAgentDevPassword


# ===== Per-env DB users =====
LANGGRAPH_DEV_DB_USER=langgraph_dev_user
LANGGRAPH_DEV_DB_PASSWORD=dev_langgraph_password
LANGGRAPH_LIVE_DB_USER=langgraph_live_user
LANGGRAPH_LIVE_DB_PASSWORD=live_langgraph_password


# ===== Private registry (immutable <git-sha> promotion) =====
REGISTRY_URL=registry.local.test/aiassistant
IMAGE_TAG=__GIT_SHA__

# ===== TeamCity database (shared Postgres) =====
TEAMCITY_DB_NAME=teamcity
TEAMCITY_DB_USER=teamcity
TEAMCITY_DB_PASSWORD=teamcity_secure_password

# ===== N8N Database & User =====
N8N_DB_NAME=n8n
N8N_DB_USER=n8n_user
N8N_DB_PASSWORD=n8n_secure_password

# ===== LiteLLM Secrets =====
LITELLM_MASTER_KEY=
LITELLM_SALT_KEY=

# ===== LiteLLM Database & User =====
LITELLM_DB_NAME=litellm
LITELLM_DB_USER=litellm_user
LITELLM_DB_PASSWORD=litellm_secure_password

# OpenObserve Database Configuration
OPENOBSERVE_DB_NAME=openobserve_db
OPENOBSERVE_DB_USER=openobserve_admin
OPENOBSERVE_DB_PASSWORD=super_secure_password

# ===== Langfuse Database & User =====
LANGFUSE_DB_NAME=langfuse
LANGFUSE_DB_USER=langfuse_user
LANGFUSE_DB_PASSWORD=langfuse_secure_password
LANGFUSE_HOST=postgres:5432
LANGFUSE_DATABASE_URL=postgresql://${LANGFUSE_DB_USER}:${LANGFUSE_DB_PASSWORD}@${LANGFUSE_HOST}/${LANGFUSE_DB_NAME}

# ===== Qdrant =====
QDRANT_KEY=qdrant_secure_password

# ===== ClickHouse (Langfuse analytics) =====
CLICKHOUSE_HOST=clickhouse:8123
CLICKHOUSE_URL=http://${CLICKHOUSE_HOST}
CLICKHOUSE_MIGRATION_URL=clickhouse://clickhouse:9000
CLICKHOUSE_DB_NAME=clickhouse
CLICKHOUSE_DB_USER=clickhouse_admin
CLICKHOUSE_DB_PASSWORD=clickhouse_secure_password

# ===== Redis =====
REDIS_HOST=redis-agents
REDIS_PORT=6379

# ===== MinIO Credentials =====
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minio_secure_password

# ===== OpenObserve Credentials =====
ZO_ROOT_USER_EMAIL=admin@example.com
ZO_ROOT_USER_PASSWORD=zo_secure_password

# ===== n8n Configuration =====
N8N_HOST=n8n.local.test
N8N_PORT=5678
N8N_WEBHOOK_URL=http://n8n.local.test
N8N_EDITOR_BASE_URL=http://n8n.local.test
N8N_SECURE_COOKIE=false
N8N_PROXY_HOPS=1
N8N_DB_TYPE=postgresdb
N8N_DB_HOST=postgres
N8N_DB_PORT=5432
N8N_DB_DATABASE=${N8N_DB_NAME}
N8N_DB_USER=${N8N_DB_USER}
N8N_DB_PASSWORD=${N8N_DB_PASSWORD}
N8N_OTEL_ENABLED=true
N8N_OTEL_SERVICE_NAME=n8n-production
N8N_OTEL_EXPORTER_OTLP_ENDPOINT=http://otel-collector:4318
N8N_LANGCHAIN_TRACING_V2=true
N8N_LANGCHAIN_API_KEY=sk-lf-fb403b7e-a2c6-40c9-b3c5-e2e4e4071516
N8N_LANGCHAIN_ENDPOINT=http://langfuse:3000/api/public/langchain
N8N_LANGCHAIN_PROJECT=cmqt5kin60001o007qiqgbw72

# ===== Langfuse Configuration =====
LANGFUSE_HOSTNAME=0.0.0.0
LANGFUSE_CLICKHOUSE_USER=${CLICKHOUSE_DB_USER}
LANGFUSE_CLICKHOUSE_PASSWORD=${CLICKHOUSE_DB_PASSWORD}
LANGFUSE_CLICKHOUSE_URL=http://${CLICKHOUSE_DB_USER}:${CLICKHOUSE_DB_PASSWORD}@${CLICKHOUSE_HOST}/${CLICKHOUSE_DB_NAME}
LANGFUSE_CLICKHOUSE_MIGRATION_URL=clickhouse://${CLICKHOUSE_DB_USER}:${CLICKHOUSE_DB_PASSWORD}@${CLICKHOUSE_HOST}/${CLICKHOUSE_DB_NAME}
LANGFUSE_REDIS_URL=redis://langfuse_redis:6379
LANGFUSE_NEXTAUTH_SECRET=langfuse_secure_password
LANGFUSE_NEXTAUTH_URL=http://lf.local.test
LANGFUSE_S3_MEDIA_UPLOAD_BUCKET=langfuse-media-bucket
LANGFUSE_S3_EVENT_UPLOAD_BUCKET=langfuse-event-bucket
LANGFUSE_S3_ENDPOINT=http://minio:9000
LANGFUSE_STORAGE_TYPE=local
LANGFUSE_SALT=LocalEnvironmentSaltStringValue123!
LANGFUSE_ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
LANGFUSE_LANGCHAIN_TRACING_V2=true
LANGFUSE_LANGCHAIN_ENDPOINT=http://lf.local.test/api/v1/otel
LANGFUSE_LANGCHAIN_API_KEY=langfuse_langchain_api_key

# ===== LangGraph Configuration =====
LANGGRAPH_HOST=langgraph
LANGGRAPH_PORT=8000
LANGGRAPH_URL=http://langgraph:8000
LANGGRAPH_DB_USER=langgraph_user
LANGGRAPH_DB_PASSWORD=langgraph_secure_password
LANGGRAPH_DB_NAME=langgraph

# ===== Code Server (dev-controller) =====
CODESERVER_PASSWORD=workspace_secure_password

# ===== AI Model API Keys =====
# Not directly needed to run, but stored for convenience
GROQ_API_KEY=groq_api_key
OPENROUTER_API_KEY=openrouter_api_key
GEMINI_API_KEY=gemini_api_key
HF_API_KEY=huggingface_api_key

GENERAL_ACCESS_KEY=litellm_general_access_key
FULL_ACCESS_KEY=litellm_full_access_key
SEARCH_API_KEY=search_api_key

LITELLM_VIRTUAL_KEY=${GITHUB_PAT}
GITHUB_TOKEN=${GITHUB_PAT}
UID=1000
GID=1000

OPENAI_API_KEY=${LITELLM_MASTER_KEY}