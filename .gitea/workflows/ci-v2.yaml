name: CI v2

on:
  push:
    branches:
      - main

  workflow_dispatch:


jobs:

  validate:

    name: Validate

    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: CI Context
        run: |
          echo "================================"
          echo "CI v2"
          echo "================================"

          echo "Repository: $GITHUB_REPOSITORY"
          echo "Commit: $GITHUB_SHA"
          echo "Started: $(date)"

      - name: Environment
        run: |
          python --version
          uv --version
          docker --version

      - name: Validate
        run: |
          echo "TODO: replace with delivery validation"