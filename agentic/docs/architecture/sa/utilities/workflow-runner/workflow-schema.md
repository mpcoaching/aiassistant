##Workflow Schema

Only the YAML.

name:

description:

version:

role:

inputs:

outputs:

(repeating section) steps:

Step

type:

name:

uses:

with:

Supported types

workflow

skill

tool

Nothing else.