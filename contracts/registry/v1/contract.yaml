name: registry
version: v1

requirements:
  REGISTRY_USER:
    required: true
  REGISTRY_PASSWORD:
    required: true
  REGISTRY_ENDPOINT:
    required: true
    default: https://registry.local.test

validators:
  - required-fields
  - registry-connectivity
  - registry-authentication
