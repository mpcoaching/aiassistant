# local.test zone - authoritative
local.test:53 {
    log
    errors
    file /etc/coredns/local.test.zone
}

# Root zone (.:53) - forward everything else
. {
    log
    errors
    forward . 1.1.1.1 8.8.8.8
    cache 30
}