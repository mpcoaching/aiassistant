# Public DNS - forward all non-local.test queries
. {
    log
    errors
    forward . 1.1.1.1 8.8.8.8
    cache 30
}

# Local DNS - authoritative for local.test
local.test {
    log
    errors
    file /etc/coredns/local.test.zone
}
