:53 {
    log
    errors

    # Handle *.local.test queries
    local.test {
        file /etc/coredns/local.test.zone
    }

    # Forward everything else (except *.local.test)
    forward . 1.1.1.1 8.8.8.8
    cache 30
}