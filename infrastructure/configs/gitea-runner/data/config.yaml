log:
  level: info

runner:
  capacity: 2
  timeout: 3h
  insecure: true

cache:
  enabled: true
  dir: /cache
  host: infra_gitea_runner
  port: 8088

container:
  image: aiassistant-ci:latest
  force_pull: false
  network: infrastructure-network